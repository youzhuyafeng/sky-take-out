<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">
    <resultMap id="DishVO" type="com.sky.vo.DishVO">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="category_id" property="categoryId"/>
        <result column="price" property="price"/>
        <result column="image" property="image"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="update_time" property="updateTime"/>
        <result column="categoryName" property="categoryName"/>
        <collection property="flavors" ofType="com.sky.entity.DishFlavor">
            <id column="dfId" property="id"/>
            <result column="dish_id" property="dishId"/>
            <result column="dfName" property="name"/>
            <result column="value" property="value"/>
        </collection>
    </resultMap>

    <insert id="saveDish" useGeneratedKeys = "true" keyProperty = "id">
        insert into dish(name, category_id, price, image,
                         description, status, create_time,
                         update_time, create_user, update_user)
        values (#{name},#{categoryId},#{price},#{image},
                #{description},#{status},#{createTime},
                #{updateTime},#{createUser},#{updateUser})
    </insert>

    <insert id="saveDishFlavorBatch">
        insert into dish_flavor(dish_id, name, value) values
        <foreach collection="flavors" item="flavor" separator=",">
            (#{flavor.dishId},#{flavor.name},#{flavor.value})
        </foreach>

    </insert>

    <select id="getDishListWithCategory" resultType="com.sky.vo.DishVO">
        select dish.*,category.name category_name from dish left join category on category_id=category.id
        <where>
            <if test="name != null">
                dish.name like concat("%",#{name},"%")
            </if>
            <if test="categoryId != null">
                dish.category_id=#{categoryId}
            </if>
            <if test="status != null">
                dish.status=#{status}
            </if>
        </where>
    </select>

    <select id="selectDishByIds" resultType="com.sky.entity.Dish">
        select * from dish where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </select>

    <delete id="deleteByIds">
        delete from dish where id in
        <foreach collection="ids" separator="," item="id" open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <delete id="deleteDishFlavorByIds">
        delete from dish_flavor where dish_id in
        <foreach collection="ids" separator="," item="id" open="(" close=")">
            #{id}
        </foreach>
    </delete>
    <update id="update">
        update dish
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="price != null">price = #{price},</if>
            <if test="image != null and image != ''">image = #{image},</if>
            <if test="description != null and description != ''">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="createUser != null">create_user = #{createUser},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
        </set>
        where id=#{id}
    </update>

    <select id="getDishByIdWithFlavor" resultMap="DishVO">
        select d.id,d.name,d.category_id,d.price,d.image,d.description,d.status,d.update_time,
               c.name categoryName,
               df.id dfId, df.dish_id, df.name dfName, df.value
        from dish d left join category c on d.category_id=c.id left join dish_flavor df on df.dish_id=d.id
        where d.id = #{id}
    </select>

    <select id="list" parameterType="Dish" resultType="Dish">
        select * from dish
        <where>
            <if test="name != null"> and name like concat('%',#{name},'%') </if>
            <if test="categoryId != null"> and category_id = #{categoryId} </if>
            <if test="status != null"> and status = #{status} </if>
        </where>
    </select>

    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from dish
        <where>
            <if test="status != null"> and status = #{status} </if>
            <if test="categoryId != null"> and category_id = #{categoryId} </if>
        </where>
    </select>
</mapper>